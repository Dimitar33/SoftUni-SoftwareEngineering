using System;
using System.Linq;

namespace _5._Longest_Increasing_Subsequence
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] n = Console.ReadLine().Split().Select(int.Parse).ToArray();

            string firstSiquence = "";
            string bigSiquence = "";

            for (int i = 0; i < n.Length; i++)
            {
                if (n[i] < )
                {

                }
            }
        }
    }
}
